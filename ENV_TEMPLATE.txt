# Evolution API Configuration
EVOLUTION_SERVER_URL=https://evo.nousdynamics.com/
EVOLUTION_API_KEY=
EVOLUTION_INSTANCE=

# Meta Marketing API Configuration
META_ACCESS_TOKEN=
META_BUSINESS_ID=2126303521185590

# Optional: Rate limiting and retry configuration
MAX_RETRIES=3
RETRY_DELAY_SECONDS=300

# Optional: DRY_RUN mode (quando true, não envia WhatsApp; apenas gera e salva em .tmp/)
DRY_RUN=false

# Espelhamento do Ads Manager (atribuição/resultados)
# action_report_time: impression | conversion | mixed
META_ACTION_REPORT_TIME=conversion
# Janelas de atribuição (csv): ex. 7d_click,1d_view
META_ATTRIBUTION_WINDOWS=7d_click,1d_view
# action_type principal para "Resultados" (ex.: lead)
REPORT_RESULT_ACTION_TYPE=lead

# Webhook n8n para notificar erros (token Meta expirado vs outros erros da automação).
# Se vazio, usa a URL padrão do projeto (Easypanel / erro-relatorio).
ERROR_WEBHOOK_URL=

TZ=America/Sao_Paulo
# Timezone para "é segunda-feira?" e fallback quando a conta Meta não expõe timezone_name
DEFAULT_REPORT_TIMEZONE=America/Sao_Paulo

# Relatorio semanal: cron na segunda 10:00 (Dockerfile). Forcar envio em qualquer dia (teste):
# FORCE_WEEKLY_REPORT=1

# Webhook leads Lorena (Make -> POST /lorena-new-lead -> WhatsApp grupo)
# Porta HTTP interna do container (mapear no Easypanel para HTTPS).
WEBHOOK_PORT=8080
# ID do grupo WhatsApp. Se vazio, usa group_id de "Lorena Carvalho" em clients.json (sincronize ao mudar o grupo).
LORENA_LEAD_GROUP_ID=
# Se preenchido, o Make deve enviar o mesmo valor em X-Webhook-Secret ou Authorization: Bearer <valor>
LORENA_LEAD_WEBHOOK_SECRET=
# Opcional: texto em "WhatsApp do Lead:" só quando o lead não tem número válido (sem dígitos).
# Deixe vazio para mostrar "(não informado)" — use só se quiser um contato fixo operacional.
LORENA_FALLBACK_WHATSAPP=

# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Preencha todas as variáveis obrigatórias com seus valores reais
# 3. O arquivo .env está no .gitignore por segurança
# 
# IMPORTANTE - Modo Multi-Client:
# - META_BUSINESS_ID: ID do Business Manager (obrigatório para o cron principal:
#   execution/main_scheduler.py lista contas do Business e cruza com clients.json)
# - Novas contas de anúncio + grupos WhatsApp: edite clients.json na RAIZ do repo
#   (um objeto por cliente: client_name, ad_account_id, group_id, enabled)
# - No Docker/Easypanel: clients.json vai na imagem (git) ou monte um volume em /app/clients.json
# - META_AD_ACCOUNT_ID / EVOLUTION_GROUP_ID: opcionais (scripts manuais e testes)
#
# PRODUÇÃO (Easypanel) — conferir também:
# - META_ACTION_REPORT_TIME, META_ATTRIBUTION_WINDOWS, REPORT_RESULT_ACTION_TYPE (alinhamento com Ads Manager)
# - WEBHOOK_PORT mapeado no proxy HTTPS; LORENA_LEAD_WEBHOOK_SECRET se quiser proteger o POST
# - LORENA_FALLBACK_WHATSAPP (opcional) e LORENA_LEAD_GROUP_ID (opcional; senão usa clients.json)
